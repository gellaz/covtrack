import 'package:flutter/material.dart';

class ChooseFirstHouseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Choose First House'),
      ),
      body: Center(
        child: Text('Choose First House'),
      ),
    );
  }
}

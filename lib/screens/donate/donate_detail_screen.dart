import 'package:flutter/material.dart';

class DonateDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Donate Detail'),
      ),
      body: Center(
        child: Text('Donate Detail'),
      ),
    );
  }
}
